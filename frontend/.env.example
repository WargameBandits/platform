# === API Backend URL ===
# 로컬 개발 (Docker Compose + Nginx 프록시):
#   미설정 시 기본값 /api/v1 사용 (Nginx가 백엔드로 프록시)
#
# Vercel 배포 (Render 백엔드와 크로스 오리진):
# VITE_API_URL=https://wargame-bandits-api.onrender.com/api/v1
